export enum blockColor {
  preto = "#000000",
  verde = "#28a745",
  amarelo = "#ffd700",
}

export enum Estado {
  editing = "editing",
  submitted = "submitted",
  default = "default",
}

export default blockColor;
